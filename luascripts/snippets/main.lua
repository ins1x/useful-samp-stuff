script_author("Your Name")
script_name("Blank")
script_description("Lua blank script")
--script_dependencies('imgui', 'lib.samp.events', 'vkeys', 'memory')
--script_url("https://github.com/profile")
script_version("1.0")

require "lib.moonloader"

function main()
    while not isSampAvailable() do wait(0) end
     
    while true do
        wait(0)
     
    end
end